JavaPlugin:
  enable: true
  localDir: JavaPlugin
ScriptPlugin:
  enable: true
  localDir: ScirptPlugin
  classPath: [
    ]
  URLPlugin:
    urls: [
      ]
message:
  load: '%plugin% is loaded'
  unload: '%plugin% is unloaded'